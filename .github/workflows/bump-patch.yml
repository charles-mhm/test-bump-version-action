on:
  push:
    tags:
      - patch

jobs:
  bump_version:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set Name
        run: |
          echo "NAME=PATCH" >> $GITHUB_ENV

      - name: Increment
        run: |
          echo "VALUE=$(($PATCH + 1))" >> $GITHUB_ENV
        env:
          PATCH: ${{ vars.PATCH }}
      
      - name: confusion
        run: echo "$PATCH" "$VALUE"

      - name: Bump patch
        run: ./bump_version.sh
        env:
          TOKEN: ${{ secrets.TOKEN }}